## scopes and clousers
variable types  = 3
var,let and const
scope types:
    block scope
    {
        let i =0;
    }
    function scope 
    function scope(){
        <!-- do something -->
    }
    scope() = this will creates the function scope
    module scope and 
    global scope

=> "var" are hosted so they can be used before initialized

<!-- global object -->
the global object is the variable window in case of browser

<!-- lexical environment -->
every variable in js (within global /block/or function ) has a reference to an object like data called  "lexical environment"

<!-- IIFE immediately invoked function expression -->

(
    function sim(){
        console.log("sjsjs")
    }
)()

<!-- copying objects -->

object.assign
... spread operator 

<!-- decorator -->

its a design pattern in which you modify the functionality of a function by covering it inside a wrapper.

<!-- debounce  -->
<!-- throttle -->
<!-- iterables and generators -->
<!-- map and set -->
<!-- WeakMap and WeakSet -->
 